This addon allows to import Second Life's Linden Lab Mesh (.llm) files into Blender.

Currently it ignores normals and binormals data.

It imports morphs as shape keys, but it can't morph texture coordinates.
